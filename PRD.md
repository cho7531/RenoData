# PRD: 등기부등본 자동 추출 시스템 (RenoData)

## 1. 제품 개요
등기부등본 PDF 파일에서 정비사업 조합원 명부 작성에 필요한 정보를 자동으로 추출하여 엑셀 파일로
변환하는 웹 애플리케이션입니다. 정비사업 초기 기본 정보 수집 단계에서 반복적인 수작업을 자동화합니다.

- 서비스 URL: https://renodata-registry-extractor.vercel.app
- 저장소: https://github.com/cho7531/RenoData

## 2. 목표 및 비전
정비사업 기본계획 수립 시 사업지 현황 정보 수집에 소요되는 시간을 대폭 감소시킵니다.

- 등기부등본 수작업 입력 제거
- 다중 필지·다중 호실 동시 처리
- 데이터 입력 오류 최소화
- 조합원 명부 표준 양식에 맞춘 결과물 즉시 생산

## 3. 주요 기능

### 3.1 PDF 파일 업로드
- 한국의 공식 등기부등본 PDF 파일 지원 (텍스트 기반 PDF)
- 단일 또는 다중 파일 일괄 업로드 (드래그 앤 드롭 지원)
- 파일 크기 제한: 1개 파일당 10MB 이하, 1회 최대 50개
- 하나의 PDF 파일 안에 여러 건의 등기부등본이 이어 붙어 있는 경우도 자동으로 건별 분리 처리

### 3.2 정보 자동 추출
다음 정보를 자동으로 추출하여 표준 조합원 명부 컬럼 구조로 정리합니다.

| 컬럼 | 설명 |
|---|---|
| 호수연번 | 부동산(호실) 단위 일련번호 |
| 토지등소유자번호 | 소유자 번호. 공유자는 N-1, N-2 형식으로 자동 부여 |
| 공동지분 | 같은 호실을 공유하는 소유자 간 지분 (예: 1/2) |
| 이름 / 생년월일 / 소유자 주소 | 갑구 기준 현재 유효 소유자 정보 |
| 우편번호 / 연락처 | 등기부등본에 없는 정보로 빈 칸 유지, 수기 보완용 |
| 해당번지 | 대지권의 목적인 토지 지번 |
| 주택명 / 동명 / 호수 | 집합건물 표시 정보 |
| 지번면적 / 소유 토지면적 | 대지면적 및 대지권비율로 계산한 소유 토지면적 |
| 건축물용도 / 건축물소유연면적 | 전유부분 건물 표시 정보 |

- 갑구에 소유권 이전 이력이 여러 건 있어도 말소되지 않은 최종 유효 소유자만 추출 (과거 소유자 자동 배제)
- 같은 호실의 공유자는 모두 포함, 지분 비율까지 인식

### 3.3 추출 결과 검수 및 수정
- 추출 직후 결과를 화면에서 바로 확인
- 셀 클릭으로 직접 값 수정 (인라인 편집)
- 잘못 추출된 행 삭제, 누락된 항목 수동 추가
- 여러 번에 나눠 업로드해도 결과가 누적되어 하나의 표로 관리

### 3.4 엑셀 파일 생성
- 표준 엑셀 형식(.xlsx), 헤더 스타일링 및 자동 열 너비 조정
- 다운로드 즉시 사용 가능한 형태로 생성

## 4. 사용자 시나리오

### As-Is (현재)
1. 등기소에서 등기부등본 발급 (다중 필지·호실의 경우 여러 장)
2. 각 건마다 지번, 면적, 소유자, 생년월일 등을 수작업으로 엑셀 입력
3. 공유자·이전 소유자 이력 등을 사람이 직접 판별해 정리
4. 타이핑 오류 발생 → 재확인 필요, 건수가 많으면 수십 분~수 시간 소요

### To-Be (개선)
1. 등기부등본 PDF 파일들을 웹에서 업로드 (여러 건 합쳐진 파일도 그대로 업로드 가능)
2. "정보 추출" 클릭 → 건당 수 초 내 자동 추출 및 정리
3. 결과를 화면에서 검수·수정
4. 엑셀 다운로드 → 바로 분석/다음 단계 진행

## 5. 기술 요구사항
- 프론트엔드: HTML5 + CSS3 + Vanilla JavaScript
- 백엔드: Python (Flask)
- PDF 텍스트 추출: pdfplumber
- 정보 구조화 추출: Anthropic Claude API (Claude Sonnet 5, 텍스트 기반)
- 엑셀 생성: openpyxl
- 배포: Vercel (Python 서버리스 함수), 로컬 실행도 지원

> 최초 기획 시 Vision(이미지) 기반 OCR을 고려했으나, 실제 등기부등본 PDF가 대부분 텍스트
> 선택이 가능한 디지털 문서라는 점을 확인하고 pdfplumber 텍스트 추출 + Claude 텍스트 분석
> 조합으로 전환하여 속도와 비용 효율을 높였습니다.

## 6. 성공 지표
- 정보 추출 정확도: 실제 등기부등본 기준 검수 후 목표 95% 이상
- 처리 시간: 건당 약 2~5초
- 다중 파일/다중 건 처리: 한 번에 다수 파일 업로드, 파일당 다중 등기부등본 자동 분리
- 사용자 작업 시간: 수작업 대비 대폭 단축

## 7. 개발 진행
- Phase 1 (MVP): Flask 백엔드 + 기본 업로드/추출/다운로드 UI
- Phase 2: 정확도 개선 (현재 소유자 판별, 공유자 지분, 다중 문서 파일 분리), 결과 검수·수정 기능, 결과 누적 처리
- Phase 3: 요청 양식(조합원 명부)에 맞춘 컬럼 재구성, 디자인 개선
- 이후: 대량 배치 처리, 정확도 추가 검증, 조합원 관리·사업성 분석 등 확장 기능 (예정)

## 8. 제약사항 및 고려사항
- 공식 등기부등본 형식에 최적화 (표제부/갑구 구조 기준)
- 스캔본 등 텍스트 추출이 안 되는 PDF는 수동 보정 필요
- API 호출 기반이므로 인터넷 연결 필수
- 우편번호·연락처처럼 등기부등본에 없는 정보는 자동 추출 불가 (수기 입력 필요)
- Vercel 무료 플랜은 서버리스 함수 실행시간 제한이 있어, 대량 파일(약 10개 이상) 동시 처리 시
  타임아웃 가능성 있음 — 로컬 실행 시에는 제한 없음

## 9. 기대 효과
정비사업 기본계획 수립 단계에서 사업지 현황 조사 시간을 단축하면서 데이터 신뢰성을 확보합니다.
조합원 명부 표준 양식에 맞춘 결과물을 바로 활용할 수 있어, 향후 조합원 관리·사업성 분석·일정
관리 등 다른 업무 자동화로 확장 가능한 플랫폼입니다.
